extends ../layout

block content
    .container
        .row
            each item in movies
                .col-md-4
                    .thumbnail
                        a(href="/movie/#{item.id}",target="_blank")
                            div(style="width:100%;height:20rem;background:url('#{item.poster}') no-repeat center center;background-size:contain;")
                        .caption
                            h3 #{item.title}
                            p: a.btn.btn-primary(href="/movie/#{item.id}", role="button") 观看预告片
